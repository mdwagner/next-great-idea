{
  "name": "next-great-idea-hasura",
  "version": "0.1.0",
  "description": "NextGreatIdea (hasura)",
  "license": "MIT",
  "scripts": {
    "start": "npm run console",
    "console": "hasura --skip-update-check console --no-browser",
    "migrate:apply": "hasura --skip-update-check migrate apply",
    "metadata:apply": "hasura --skip-update-check metadata apply",
    "migrate": "npm run migrate:apply && npm run metadata:apply"
  },
  "devDependencies": {
    "hasura-cli": "^1.2.1"
  }
}
