{
  "name": "next-great-idea-hasura",
  "version": "0.1.0",
  "description": "NextGreatIdea (hasura)",
  "license": "MIT",
  "scripts": {
    "console": "hasura --skip-update-check console --no-browser",
    "migrate": "hasura --skip-update-check migrate apply && hasura metadata apply"
  },
  "devDependencies": {
    "hasura-cli": "^1.2.1"
  }
}
