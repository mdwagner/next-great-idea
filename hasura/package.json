{
  "name": "next-great-idea-hasura",
  "version": "0.1.0",
  "description": "NextGreatIdea (hasura)",
  "license": "MIT",
  "scripts": {
    "console": "hasura console",
    "migrate": "hasura migrate apply && hasura metadata apply"
  },
  "devDependencies": {
    "hasura-cli": "^1.2.1"
  }
}
